html {

    include mime.types;

    server {
        listen 443;
        root #path;

        location #dir {

            root #path;
            alias /
            try files / #path / index.html =404;
        }
    }
}

event {}